# Release binaries

| File | Purpose |
|------|---------|
| `nde-inspection-1.1.0-release.aab` | Signed Play App Bundle for Internal testing (`versionCode` 2) |

Upload this `.aab` in Play Console → Testing → Internal testing.
